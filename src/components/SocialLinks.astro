---
import Link from "./shared/Link.astro";
import { socials } from "@/data/socials";
---

<ul role="list" class="flex flex-row gap-2">
  {
    socials.map((social, index) => (
      <>
        <li>
          <Link href={social.link} label={social.label} />
        </li>

        {socials.length - 1 !== index && <li>/</li>}
      </>
    ))
  }
</ul>
